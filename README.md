# MedGuide Mobile App (roadmap)

React Native / Expo app — same Supabase backend + same `/api/*` routes as the web app.
Planned screens mirror `src/apps/web-app/pages/`: Login (OTP) → Splash → Home → Symptom Checker → Triage → Clinics → Medicines → Video → Records → SOS.
