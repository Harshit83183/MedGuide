# MedGuide Admin Dashboard (roadmap)

Clinic/doctor verification queue, testimonial approvals, SOS alert monitor, subscription reports.
Reads the same Supabase tables (`clinics`, `doctors`, `testimonials`, `sos_alerts`, `subscriptions`).
